根据我们刚刚的聊天，我已经有了大致的设计方向，你在看完下面的需求后，结合最开始生成的account-capacity-agent-framework.md，帮我重新生成一个agent设计框架.md，放到同一目录下。

整体架构采用langchain

先设计agent中的LLM，请求url和key放到环境变量下，采用多level模式，比如说level 1选用性能较好的gpt5.5，用来作为决策核心，level 2选用简单的模型5.3mini，负责执行简单的工具调用比如获取数据、通过机器人发出警告等

决策核心能通过API账号池状态和账号探测提供的数据进行决策，来判断下一步的走向，工作流我先暂定为：我发出指令->level 1模型判断指令后决定采用工具->level 2模型负责执行工具后将结果发给level 1->level 1模型根据指令和工具执行结果决策，这个工作流先这样，后续你能帮我优化优化

工具的话目前我只想到了3个：
1.API账号池状态数据获取，可以沿用现成的
2.账号探测数据获取，可以沿用现成的
3.决策下发，这个我不知道是接入钉钉机器人发送还是通过对话发送

如果就按上面这么设计的话，其实我感觉还少了点东西，如果现有的告警逻辑向钉钉发送警告了，agent应该能捕获警告并立即获取数据进行决策，由于不是我主动对话，决策应该通过钉钉机器人发送到群聊，但是我不知道agent能否一直运行就像定时刷新sub2api缓存那样，我更希望可以一直运行

agent对话界面放到该面板的Agent分析模块下

我暂时就想到这么多，该项目主负责人告诉我这个agent模块兼容性和拓展性应该非常高，我还有很多不懂的地方，后续需要你来指导我。后续一些更改比如逻辑重做等都放到agent设计框架.md中就好，先不修改文件，就先加一个agent设计框架.md